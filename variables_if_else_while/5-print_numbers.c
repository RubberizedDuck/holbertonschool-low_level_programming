#include <stdio.h>

/**
*main - Function to print all single digit numbers of base 10
* starting at 0
*Return: Always returns 0
*/

int main(void)
{
int num = 0;
while (num < 10)
{
printf(num);
num++;
}
return (0);
}
