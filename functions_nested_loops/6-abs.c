#include "main.h"
#include <stdlib.h>

/**
 * _abs - Computes the absolute value of an integer
 *@j: is the integer being used to convert to absolute value
 *Return: returns absolute value of integer
 */

int _abs(int j)
{
	int a = abs(j);

	return (a);
}
